# --- Web Framework ---
fastapi == 0.124.2
uvicorn[standard] == 0.34.0

# --- Data Validations and Settings ---
pydantic == 2.10.3
pydantic-settings == 2.7.0

# --- Firebase (Free Tier: Auth, FCM, App Check) ---
firebase-admin == 7.1.0

# --- Database (PostgreSQL) ---
sqlalchemy[asyncio] == 2.0.36
asyncpg == 0.30.0

# --- Data Science and ML ---
numpy == 2.1.3
pandas == 2.2.3
scikit-learn == 1.6.0
joblib == 1.4.2

# --- Deep Learning ---
# We use the CPU version to save ~1GB of space.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1

# --- Utilities ---
python-dotenv == 1.0.1
httpx == 0.28.1
aiofiles == 24.1.0

# --- Scheduling ---
apscheduler == 3.10.4

# --- Security & Rate Limiting ---
slowapi == 0.1.9

# --- Testing ---
pytest == 8.3.4
pytest-asyncio == 0.25.0
